

namespace LostGen.Pawn {
    public static class Reducers {
        public static PawnState PawnReducer(PawnState previousState, Actions.PawnAction action) {
            PawnState nextState = previousState;
            return nextState;
        }
    }
}